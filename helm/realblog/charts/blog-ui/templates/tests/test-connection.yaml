apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "blog-ui.fullname" . }}-test-connection"
  labels:
    {{- include "blog-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "blog-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
